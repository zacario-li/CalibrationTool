#pragma once
#include <wx/panel.h>

class TabStereoDisparityPanel : public wxPanel {
public:
    TabStereoDisparityPanel(wxWindow* parent);
    // Add methods and members specific to this tab later
};
